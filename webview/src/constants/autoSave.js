/**
 * Shared debounce delay for auto-save (case edit, run description, run pass/fail).
 * Aligned with ~2s used by Obsidian; reduces write load while keeping feel consistent.
 */
export const DEBOUNCE_MS = 2000;
