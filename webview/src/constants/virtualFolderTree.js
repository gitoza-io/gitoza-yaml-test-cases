/** Minimum visible folder rows before virtual scrolling is enabled. */
export const VIRTUAL_FOLDER_TREE_THRESHOLD = 80;

/** Estimated folder row height (px): single-line label + badge. */
export const FOLDER_ROW_ESTIMATE_PX = 36;

/** Extra rows rendered above/below the viewport. */
export const VIRTUAL_FOLDER_TREE_OVERSCAN = 8;
