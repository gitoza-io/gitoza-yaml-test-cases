/** Minimum diff lines before virtual scrolling is enabled in DiffViewer. */
export const VIRTUAL_DIFF_VIEWER_THRESHOLD = 100;

/** Estimated single-line row height (px): line-height + vertical padding. */
export const DIFF_LINE_ESTIMATE_PX = 24;

/** Extra diff lines rendered above/below the viewport. */
export const VIRTUAL_DIFF_VIEWER_OVERSCAN = 12;

/** Minimum diff lines before the large-run guidance banner is shown (Confirm Changes). */
export const LARGE_RUN_DIFF_BANNER_THRESHOLD = 500;
