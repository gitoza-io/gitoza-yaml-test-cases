/** Minimum case count in a folder before virtual scrolling is enabled. */
export const VIRTUAL_CASE_LIST_THRESHOLD = 40;

/** Estimated row height (px): TestCaseIcon h-8 + CaseRowLabel two lines + padding. */
export const CASE_ROW_ESTIMATE_PX = 44;

/** Extra rows rendered above/below the viewport. */
export const VIRTUAL_CASE_LIST_OVERSCAN = 8;
