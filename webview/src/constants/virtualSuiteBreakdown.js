/** Minimum suite rows before virtual scrolling is enabled in RunSuiteBreakdownList. */
export const VIRTUAL_SUITE_BREAKDOWN_THRESHOLD = 40;

/** Estimated row height (px): header + stack bar + padding. */
export const SUITE_BREAKDOWN_ROW_ESTIMATE_PX = 52;

/** Max height of the scrollable suite breakdown list (px). */
export const SUITE_BREAKDOWN_MAX_HEIGHT_PX = 256;

/** Extra rows rendered above/below the viewport. */
export const VIRTUAL_SUITE_BREAKDOWN_OVERSCAN = 6;

/** Vertical gap between rows (px), included in virtual row stride. */
export const SUITE_BREAKDOWN_ROW_GAP_PX = 8;
