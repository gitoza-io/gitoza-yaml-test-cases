{
  "name": "gitoza-yaml-test-cases",
  "displayName": "YAML Test Cases & Runs — QA for VS Code",
  "description": "Manage manual test cases and runs as YAML in your repo. Browse suites, edit cases, execute test runs — AI-friendly plain files for Cursor, Copilot & ChatGPT.",
  "version": "0.2.0",
  "publisher": "gitoza",
  "license": "MIT",
  "icon": "media/128x128.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/gitoza-io/gitoza-yaml-test-cases.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Testing"
  ],
  "keywords": [
    "test case",
    "test cases",
    "manual testing",
    "qa",
    "quality assurance",
    "test management",
    "test plan",
    "test suite",
    "test run",
    "yaml",
    "test repository",
    "cursor",
    "copilot",
    "ai",
    "gitoza"
  ],
  "galleryBanner": {
    "color": "#1e1e2e",
    "theme": "dark"
  },
  "activationEvents": [
    "workspaceContains:.gitoza-lite/test/cases",
    "onView:gitoza.launcher",
    "onCommand:gitoza.openTestRepository"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "gitoza.openTestRepository",
        "title": "Gitoza: Open Test Repository",
        "icon": "$(file-code)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "gitoza",
          "title": "Gitoza",
          "icon": "media/activitybar.png"
        }
      ]
    },
    "views": {
      "gitoza": [
        {
          "id": "gitoza.launcher",
          "name": "Gitoza",
          "type": "tree"
        }
      ]
    }
  },
  "scripts": {
    "build:ext": "node esbuild.mjs",
    "build:webview": "cd webview && npm run build",
    "build": "npm run build:ext && npm run build:webview",
    "watch:ext": "node esbuild.mjs --watch",
    "watch:webview": "cd webview && npm run dev",
    "watch": "npm run watch:ext & npm run watch:webview",
    "test": "vitest run",
    "test:ext": "vitest run --config vitest.config.ts",
    "package": "npm run build && vsce package --no-dependencies",
    "vscode:prepublish": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.2.1",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "yaml": "^2.6.1"
  }
}
